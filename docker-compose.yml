version: '3'
services:
  people-info-api:
    image: ibrahimsobhy/hadeeth-pro-people-info
    build: .
    environment:
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_USER=postgres
      - POSTGRES_PWD=postgres_password

  postgres:
    image: postgres